// import * as os from "os";

// // console.debug(`
// //     os.platform: ${os.platform}
// //     process.env.USER: ${process.env.USER}
// //     process.env.USERNAME: ${process.env.USERNAME}
// //     process.env.USERPROFILE: ${process.env.USERPROFILE}
// // `);

// let userName = process.env.USER || process.env.USERNAME;
// if (!userName) {
//   userName = `${Math.floor(Math.random() * Date.now())}`;
// }

// const lowerOnly = (frag: string) =>
//   frag.toLowerCase().replace(/[^a-z0-9]/g, "");

// export const UserName = lowerOnly(userName);
